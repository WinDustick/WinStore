feat(data, docs): Внедрение генерации данных и единой документации

1. Система генерации данных
- Добавлены скрипты в `scripts/data_generation/` для наполнения БД тестовыми данными (CPU, GPU).
- Реализован раздельный подход к генерации GPU NVIDIA (GTX/RTX и GT) для повышения надежности (см. ADR-2025-10-19).
- Добавлены зависимости `requests`, `beautifulsoup4`, `Faker`.

2. Единая система документации
- Создана директория `docs/aic_guide/` с высокоуровневой документацией по архитектуре, БД и процессам разработки.
- Новая документация служит единой точкой входа и связывает существующие документы.

3. Улучшения инфраструктуры и БД
- Docker: Упрощен `docker-compose.yml` с использованием единого профиля `app`.
- Ожидание БД: Переработан скрипт `backend/wait_for_db.py` для надежной поддержки MSSQL и Oracle.
- Oracle: Улучшены скрипты развертывания и сброса; добавлены диагностические представления (`view_product_full_details`).
- Зависимости: Обновлена библиотека `Pillow` до v12.0.0.
